#include<bits/stdc++.h>
#define endl '\n'
using namespace std;
using PII = pair<int, int >;
using LL = long long;
// const int N = 2e5 + 5;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, m;
    cin >> n >> m;
    char t;
    for(int i = 0; i < m; i ++ ){
        char c;
        int x;
        cin >> c >> x;
        t = c;
    }
    int cnt = 1000;

    while(cnt -- ){
        cout << t;
    }
    cout << endl;

    return 0;
}